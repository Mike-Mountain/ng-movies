import { SetImageSrcPipe } from './set-image-src.pipe';

describe('SetImageSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new SetImageSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
